#ifndef __ceanos_kout__
#define __ceanos_kout__

void kout(const char *s);       // kernel out
void koutd(const char *s);      // kernel out debug
void koutw(const char *s);      // kernel out warning
void koute(const char *s);      // kernel out error

#endif
